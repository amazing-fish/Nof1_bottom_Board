# 项目锚点

## 项目概览
- 名称：Nof1 Bottom Board
- 主要组件：`AlphaBoard.js`
- 当前版本：`v1.3.1`（bugfix）

## 当前目标
- 当前任务：修复 review 指出的主面板行情回归：主面板 BTC/XAU 在次面板收起状态下也必须持续轮询更新。
- 行为约束：仅在面板所在标签页位于前台时执行实时更新（含折叠/展开状态下的时间与数据刷新）。
- 兼容策略：主面板行情轮询与次面板展开态解耦；次面板继续仅在展开态触发补偿刷新。

## 约束
- 遵循 `AGENTS.md` 中列出的沟通、流程与审计要求。
- 维护轻量、可读的前端代码结构。
- 版本号按 `v主.次.修` 维护；本次属于 bugfix，升级到 `v1.3.1`。

## 设计笔记
- 主面板行情轮询修正：移除 `startFeatureTickerPollers` 内对 `FEATURE_EXPANDED` 的提前返回门控，避免次面板收起时阻断主面板 BTC/XAU 的数据拉取。
- 次面板交互保持：保留展开次面板时触发 `featurePollers` 立即刷新与次面板时间文案展开态更新逻辑。
- 可见性约束不变：主/次面板轮询均仍受 `isTabForeground()` 控制，后台标签页保持低频等待。

## 验证记录
- 2026-02-10：修复主面板轮询门控回归，执行 `node --check AlphaBoard.js`，语法检查通过。
- 2026-02-10：执行 `node --check AlphaBoard.js`，验证 v1.3.1 bugfix 语法与脚本结构，检查通过。
- 2026-02-10：修复模型卡重排容器，执行 `node --check AlphaBoard.js`，语法检查通过。
- 2026-02-10：执行 `node --check AlphaBoard.js`，语法检查通过。

## 潜在影响
- 主面板 BTC/XAU 在次面板收起状态下恢复持续更新，避免默认视图数据冻结。
- 次面板展开态补偿刷新与时间文案节流行为保持不变，资源优化策略延续。
- 轮询门控调整仅作用于主面板 ticker 轮询，不影响链上模型卡的更新路径。
